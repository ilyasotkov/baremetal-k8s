releases:

- name: fission
  installed: false
  namespace: kube-system
  labels:
    app: fission
  chart: fission/fission-all
  values:
  - nats:
      enabled: false
    persistence:
      enabled: true
      storageClass: rook-ceph-block
      accessMode: ReadWriteOnce
      size: 4Gi
